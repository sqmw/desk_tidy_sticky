# 2026-01-27 — 依赖对齐 desk_tidy

## 做了什么
- 将 desk_tidy_sticky 的 Dart/Flutter SDK 约束提升到 `^3.8.1`，与同级项目 desk_tidy 保持一致。
- 使用 FVM 将本项目锁定到 Flutter 3.38.7（Dart 3.10.7），满足新的 SDK 约束并成功 `fvm flutter pub get`。

## 操作命令
```bash
cd f:\language\dart\code\desk_tidy_sticky
fvm use 3.38.7 --force
fvm flutter pub get
```

## 后续建议
- 其他机器或 CI 需要安装对应 FVM 版本；若不用 FVM，则直接使用 Flutter 3.38.7+ 运行。
- 若 desk_tidy 后续升级依赖，记得同步 desk_tidy_sticky 的重叠依赖版本以免微差。
