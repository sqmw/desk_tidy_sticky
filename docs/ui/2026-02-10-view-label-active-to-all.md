# 视图命名优化：活动 -> 全部

## 调整
- 将视图标签 `活动` 改为 `全部`，英文 `Active` 改为 `All`。

## 原因
- “活动”语义不直观，用户难以第一时间理解是“全部未归档项”。
- “全部”更符合常见信息架构命名习惯，减少认知负担。

## 影响文件
- `src/lib/strings.js`

## 验证
- 执行：`npm run check`
- 结果：`0 errors, 0 warnings`
