# 2026-02-09 Sticky 关闭入口收敛（移除 X）

## 问题
工具栏同时存在“取消钉住”和 `X` 两个看似都能关闭贴纸的入口，语义重复。  
用户容易误解两者差异，交互成本偏高。

## 调整
- 文件：`src/routes/note/[id]/+page.svelte`
- 直接移除工具栏 `X` 按钮。
- 保留“取消钉住”图标作为唯一“从桌面移除贴纸”入口。

## 结果
1. 关闭入口唯一化，避免重复操作项。
2. 行为与主界面 pin 状态保持一致。
3. 工具栏更简洁，减少误操作。
