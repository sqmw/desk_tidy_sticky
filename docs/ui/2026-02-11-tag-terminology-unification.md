# 标记/Tag 术语统一（2026-02-11）

## 背景

工作台中同时出现“标记 / 优先级 / 标签”会造成理解负担，用户认知不一致。

## 本次调整

统一交互术语为“标签（Tag）”：

1. 原“优先级”文案统一改为“标签”。
2. 原“四象限”下拉入口文案统一改为“标签”。
3. 原“未标记”统一改为“无标签”。

## 说明

为避免回归风险，本次只做交互层术语统一；底层数据字段仍沿用 `priority`（兼容已有数据与逻辑），后续可再做结构升级迁移。

## 涉及文件

- `src/lib/strings.js`
