# Workspace 左侧文案调整：今日截止任务 -> 今日任务（2026-02-11）

## 背景
- 左侧卡片标题原为“今日截止任务”。
- 当前卡片同时承载了进行中、待开始、已超时等状态，不仅仅是“截止”语义。

## 调整
- 将文案统一调整为“今日任务”。
- 修改位置：`src/lib/strings.js`
  - `zh.workspaceDeadlineTitle`: `今日截止任务` -> `今日任务`

## 结果
- 语义更中性，覆盖“今日任务”全状态展示，不再让用户误以为仅显示截止项。
