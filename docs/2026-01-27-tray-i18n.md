# 2026-01-27 — Tray 菜单中英文同步

## 问题
- 托盘菜单由 `system_tray` 构建，之前文案硬编码英文，不会随应用语言切换更新。

## 方案
- TrayService 接入 `LocaleController`，监听语言变化并重建 ContextMenu。
- 文案统一来源：`lib/l10n/strings.dart`（新增 tray 相关 key）。

## 影响
- 子进程 overlay（`--child`）不初始化托盘，因此不会出现多份托盘菜单。
