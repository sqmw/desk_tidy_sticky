# 2026-01-27 — UI 文案中英适配

## 目标
- 面板/Overlay 里所有可见文案与 tooltip 支持中英文切换。
- 与 `strings.dart` 的语言切换保持一致，避免散落硬编码英文。

## 变更点
- 扩展 `lib/l10n/strings.dart`：新增 `cancel/save/clear/hideWindow` 等通用文案。
- 面板 Header/列表/编辑弹窗使用 `Strings` 文案。
- Overlay 工具条与卡片 tooltip 使用 `Strings` 文案。

## 校验清单
1) 右上角语言切换后，按钮/提示文案同步更新。
2) 编辑弹窗/Overlay 卡片 tooltip 同步更新。
